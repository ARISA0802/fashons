<div class="container">
	<h1>会員登録ユーザー編集</h1>
	<%= form_for(@user, url:"/admin/managers") do |f| %>
    <h4>User NAME</h4>
    <%= f.text_field :name %>
    <h4>Email</h4>
    <%= f.text_area :email %>
   <p>
    <%= f.submit 'Update Book' %>  </p>

<% end %>

<%= link_to "show",admin_manager_path(@user.id) %>
</div>

