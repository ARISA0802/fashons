<div class="container">  <!-- ここを追加 -->
	<div class="row">
		<h1>
			会員登録ユーザー一覧　
		</h1>
			<table class="table table-striped">
				<thead>
					<tr>
						<th>
							ユーザー名
						</th>
						<th>
							ユーザーのメールアドレス
						</th>
						<th>
							ユーザーのアドレス
						</th>
					</tr>
				</thead>
				<% @users.each do |f| %>
				<tr>
					<td>
						<%= f.name %>
					</td>
					<td>
						<%= f.email %>
					</td>
					<td>
						<%= f.address %>
					</td>
					<td>
						<%=link_to "編集",edit_admin_manager_path(user.id) %>
					</td>
					<td>
						<%=link_to "削除",admin_manager_path(user.id),method: :delete , data:{confirm:  "本当に？"} %>
					</td>
				</tr>
				<% end %>
			</table>
	</div>
</div>
				

